import { Spin } from 'antd'
import { useIsFetching, useIsMutating } from '@tanstack/react-query'

export default function CentralLoader() {
  // Fullscreen loading screen disabled per request
  return null
}

